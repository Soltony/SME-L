/**
 * The path the previous SME deployment registered with the gateway as its
 * CALLBACK_URL, kept so an existing gateway configuration keeps working.
 */
export { POST } from '../payment/callback/route';

export const dynamic = 'force-dynamic';
